version: "3"

services:
  client:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - server

  server:
    build: ./tree_app
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
